# User Setting Area
title: LolliNeon
email: seokbong60@gmail.com
baseurl: "/lollineon" # the subpath of your site, e.g. /blog
url: "https://sweetcase-production.github.io" # the base hostname & protocol for your site, e.g. http://example.com

nickname: recoma
user_description: Lightmode is Lollipop, and darkmode is Neon Sign
profile_image: /assets/img/profile.jpg

social:
  github: https://github.com/recoma96
  email: seokbong60@gmail.com


# System Setting Area (Don't touch this!)

# Build settings
theme: minima
plugins:
  - jekyll-feed
  - jekyll-paginate

# blog system configs
paginate: 10
paginate_path: "/posts/:num/"

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge
  math_engine: mathjax

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
